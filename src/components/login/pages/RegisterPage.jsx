import React from 'react';
import MainTemplate from '../auth/MainTemplate';
import RegisterForm from '../auth/RegisterForm';

const RegisterPage = () => {
  return (
    <MainTemplate>
      <RegisterForm />
    </MainTemplate>
  );
};

export default RegisterPage;